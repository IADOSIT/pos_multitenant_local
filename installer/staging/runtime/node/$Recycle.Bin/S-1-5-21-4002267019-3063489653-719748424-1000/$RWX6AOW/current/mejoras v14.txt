Contexto:
Proyecto Laravel EMC / iados con panel admin y side menu funcional.
Las secciones /ordenes-del-dia y /operaciones pierden el side menu y parte del diseño porque usan un layout distinto (pantalla full-width).

Objetivo:
Homologar estas secciones al layout principal del admin, manteniendo side menu, estilos y UX operativa.

Tareas (no preguntar, solo ejecutar):

1) Identificar vistas Blade de:
   - /ordenes-del-dia
   - /operaciones
   - ops/hub/*
   - ops/entregas/*

2) Detectar el layout actual y crear si no existe:
   - resources/views/layouts/ops.blade.php
   - Debe extender el layout admin principal
   - Debe conservar side menu, header y assets

3) Actualizar todas esas vistas para usar:
   @extends('layouts.ops')

4) Mantener UX de pantallas operativas (full width interno permitido), SIN eliminar sidebar.

5) Ajustar CSS solo a nivel contenedor si hay overflow o ruptura visual.

6) CRÍTICO:
   Mantener integridad multi-empresa (multi-tenant):
   - No romper scoping por empresa
   - Respetar empresa activa (empresa_id)
   - No exponer datos de otra empresa
   - No modificar middlewares, guards ni filtros existentes

Resultado esperado:
- /ordenes-del-dia y /operaciones muestran el mismo side menu que el resto del admin.
- Diseño consistente, sin regresiones.
- Integridad entre empresas garantizada.
- Código limpio y alineado a buenas prácticas Laravel 10+.
