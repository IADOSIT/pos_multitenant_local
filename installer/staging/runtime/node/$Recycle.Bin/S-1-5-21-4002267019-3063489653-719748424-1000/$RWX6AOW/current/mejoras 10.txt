Eres Claude Code y YA conoces el repo emc_abastos y el contexto multi-empresa/tienda, plantillas por empresa, store_domains, portal→tienda/producto. NO reanalices el repo, NO toques lógica/queries/DB ni rutas; solo UI/CSS/Blade/Livewire views y assets. Objetivo Paso 1: aplicar un rediseño “lujoso, profesional, marketing” al STOREFRONT de TODAS las tiendas (nuevas y existentes) con layout full-width (100vw), responsive mobile-first, y sin regresiones.

1) Implementa Tailwind CSS (si ya existe, reutiliza config) y crea un layout base único del storefront (ej. layouts/storefront.blade.php) que envuelva todas las páginas de tienda: home, listado, detalle, carrito/checkout. Diseño: hero full-bleed con banner 100% ancho, overlay elegante, tipografía moderna (Inter/Manrope), cards premium (rounded-2xl, shadow suave), micro-interacciones ligeras (hover, fade-in), navegación sticky limpia, footer completo; mantener performance (purge/content, imágenes lazy-load). La tienda debe ocupar todo el ancho de pantalla (sin contenedor estrecho), con grid responsive (1 col móvil, 2–3 tablet, 4+ desktop) y botones/inputs grandes para móvil.

2) Theming sin lógica: aplica estilo por empresa usando CSS variables en el layout (ej. --brand-primary, --brand-accent, --brand-bg, --brand-text, --banner-image) alimentadas por la config/plantilla de Empresa ya existente; si no hay config, usa paleta default premium (verde esmeralda + gris carbón + blanco) con contraste AA. Banner/Flyer full-width: <section class="w-screen relative left-1/2 right-1/2 -ml-[50vw] -mr-[50vw]">…</section> para full-bleed en Blade. No cambies reglas de negocio: solo lee config ya disponible y aplica fallback.

3) FIX CRÍTICO imágenes en tienda: corrige el render de imágenes de productos en storefront (listado y detalle). Usa una única función/helper de presentación: si product.image_url existe úsala; si image_path local usa Storage::url(image_path); si falta, usa placeholder /images/placeholder-producto.webp. Agrega loading="lazy", width/height, object-cover, y evita rutas rotas (no <img src="">). Verifica que storage:link sea compatible con el disco actual y que las vistas usen el método correcto (Storage::url vs asset). No cambies el modelo/servicios, solo el render y paths.

Entrega: nuevo layout premium storefront + componentes Blade (x-navbar, x-product-card, x-banner, x-footer) o partials equivalentes, aplicados a vistas existentes sin tocar controladores; mejoras de CSS/Tailwind config; imágenes funcionando en tienda. Mantén todas las funcionalidades y navegación intactas.
